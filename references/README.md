# Frontend References

Use this file to track design references for the `frontend/` implementation.

## How To Add A Reference

1. Create a folder or file in `references/` with a descriptive name.
2. Add one entry in the `Reference Catalog` section.
3. Keep notes short and specific so implementation decisions are clear.

## Reference Catalog

### 1) link2pay-reference-angie
- `path`: `references/link2pay-reference-angie`
- `source_url`: `<add original URL or repo link>`
- `use_for`: `<layout, spacing, card patterns, etc.>`
- `avoid`: `<what not to copy>`
- `priority`: `high`
- `notes`: `<extra constraints or observations>`

### 2) stellar-pay-esteban
- `path`: `references/stellar-pay-esteban`
- `source_url`: `<add original URL or repo link>`
- `use_for`: `<hero style, typography, color system, etc.>`
- `avoid`: `<what not to copy>`
- `priority`: `high`
- `notes`: `<extra constraints or observations>`

## Quick Template (Copy/Paste)

### N) <reference-name>
- `path`: `references/<folder-or-file-name>`
- `source_url`: `<url>`
- `use_for`: `<specific UI pieces>`
- `avoid`: `<limitations>`
- `priority`: `high|medium|low`
- `notes`: `<optional>`
